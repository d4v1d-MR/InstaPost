/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { InstaPostApp } from './src/InstaPostApp';

AppRegistry.registerComponent(appName, () => InstaPostApp);
